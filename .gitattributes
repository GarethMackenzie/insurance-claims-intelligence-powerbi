# Keep GitHub language statistics aligned with the repository's primary analytical artifacts.
*.tmdl linguist-detectable=true

# Python under scripts/ supports generation, QA and reproducibility. Keep it in the repository,
# but exclude it from the language summary so the pinned card reflects the Power BI model work.
scripts/*.py linguist-detectable=false
